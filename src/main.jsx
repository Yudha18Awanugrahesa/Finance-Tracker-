import React from "react";
import ReactDOM from "react-dom/client";
import FinTrackApp from "./FinTrack"; // Sesuaikan lokasi folder FinTrack.jsx Anda

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <FinTrackApp />
  </React.StrictMode>,
);
